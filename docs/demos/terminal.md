# Build and live pci_cfg tracking

Asciinema of `make -C build` followed by live QEMU `pci_cfg_*` lines and a `pcieshark` summary.

![Building pcieshark and tracking live QEMU pci_cfg TLPs](terminal-demo.gif)
